{
  "name": "phaser-demo",
  "version": "1.0.0",
  "description": "a demo project to learn phaser game engine",
  "main": "index.js",
  "scripts": {
    "clean":"rm -r ./dist",
    "copy": "copy",
    "build": "npm run clean && tsc && copy assets/* dist/assets/ && copy index.html dist",
    "build:watch": "tsc -w && copy assets/* dist/assets && copy indx.html dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "copy": "^0.3.2",
    "phaser": "^3.80.1",
    "tsc": "^2.0.4",
    "typescript": "^5.5.4"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
